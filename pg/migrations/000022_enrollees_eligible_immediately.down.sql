-- Not reversible. The dates this moved forward were derived from
-- fund.next_payment at each enrollee's original enrollment time, and that value
-- has since changed; the originals are not recoverable from anything still
-- stored.
SELECT 1;
